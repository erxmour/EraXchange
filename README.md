# Currency Exchange Assistant (Telegram Mini App & Chatbot)

## Описание Проекта

**Currency Exchange Assistant** — это многофункциональный Telegram-бот, разработанный как удобный инструмент для конвертации валют и получения информации об обменных курсах. Проект сочетает в себе:
1.  **Telegram Mini App (TMA):** Интерактивный веб-интерфейс для быстрого и удобного расчета.
2.  **ИИ-Ассистент (Gemini):** Возможность обрабатывать сложные текстовые запросы на конвертацию и поддерживать свободный диалог на тему валют.

Проект разработан на Python с использованием фреймворка Flask, библиотеки pyTelegramBotAPI и API Google Gemini.

---

## 🌟 Основные Функции

* **Конвертер Валют в Чат-Интерфейсе:** Пользователь может просто написать, например, "500 долларов в тенге", и бот выполнит расчет, используя возможности Gemini для парсинга запроса.
* **Telegram Mini App (TMA):** Быстрый, современный интерфейс для выбора валют и ввода суммы без необходимости покидать Telegram.
* **ИИ-Ассистент (Чат):** Бот поддерживает свободный диалог, выступая в роли эксперта по валютам и помощника, который может ответить на общие вопросы о курсах, обмене и финансовых новостях.
* **Кэширование Курсов:** Обменные курсы кэшируются для ускорения ответа и снижения нагрузки на внешний API.
* **Надежность:** Реализована устойчивая обработка ошибок API (как Exchange Rate, так и Gemini).

---

## 🛠️ Технологии

* **Бэкенд:** Python
* **Веб-Фреймворк:** Flask
* **Telegram:** pyTelegramBotAPI
* **ИИ/НЛП:** Google Gemini API
* **Обменные Курсы:** ExchangeRate-API (или аналогичный)
* **Развертывание:** Gunicorn, Render (или другой PaaS)

---

## ⚙️ Запуск и Развертывание

### 1. Требования

Для запуска проекта вам потребуются следующие ключи API:
* **TELEGRAM\_BOT\_TOKEN** (от BotFather)
* **EXCHANGE\_RATE\_API\_KEY** (от [ExchangeRate-API](https://www.exchangerate-api.com/))
* **GEMINI\_API\_KEY** (от [Google AI Studio](https://aistudio.google.com/))

### 2. Подготовка Окружения

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [ВАШ АДРЕС РЕПОЗИТОРИЯ]
    cd currency-exchange-assistant
    ```

2.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

3.  **Создайте файл `.env`** в корневой папке и добавьте в него ваши ключи:
    ```
    # .env
    TELEGRAM_BOT_TOKEN=ВАШ_ТОКЕН_BOTFATHER
    EXCHANGE_RATE_API_KEY=ВАШ_КЛЮЧ_EXCHANGERATE
    GEMINI_API_KEY=ВАШ_КЛЮЧ_GEMINI
    ```

### 3. Локальный Запуск (Тестирование)

Для локального тестирования бот запускается в режиме Polling:
```bash
python eraXChange.py